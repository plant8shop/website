# 地図脚本プロトタイプの更新方法

## 管理画面を開く

通常は `管理画面を開く.cmd` をダブルクリックしてください。PowerShellの実行制限を一時的に回避し、ローカル管理サーバーと管理画面を起動します。

コマンドから起動する場合は、Windows PowerShellで `start-place-fiction-admin.ps1` を実行します。

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File ".\start-place-fiction-admin.ps1"
```

ローカル専用サーバーが `127.0.0.1:8765` で起動し、管理画面がブラウザで開きます。外部の端末からは接続できません。作業後は管理画面右上の「終了」を押してください。

## 更新の流れ

1. 左の一覧から脚本を選びます。
2. 地図をクリックするか、緯度・経度を入力して位置を設定します。
3. 題名、概要、登場人物、小道具、脚本、写真を編集します。
4. 公開する場合は公開状態を「公開」にします。題名、位置、脚本本文が不足していると保存できません。
5. 「保存」を押し、「公開ページをプレビュー」で表示を確認します。

保存先は `data/place-fiction.json` と `assets/place-fiction/<spot-id>/` です。保存は一時ファイルを経由して置換され、直前のJSONは `.admin-backups/` にローカルバックアップされます。

管理画面はGitのコミットやpushを行いません。公開反映は内容を確認したあと、従来のGit操作で別途行ってください。
